import styled from "styled-components";

const StatsContainerWrapper = styled.div`
  width: 100%;
  background-color: blue;
`;

export default function StatsContainer() {
  return <StatsContainerWrapper>StatsContainer</StatsContainerWrapper>;
}
